---
import Center from "../Center.astro";
---

<footer>
  <Center>
    <div class="content">
      <!-- 左列：Connect -->
      <div class="connect">
        <h2 class="title">Connect</h2>
        <ul>
          <li>
            <a href="https://github.com/Yoko0709" target="_blank" rel="noopener noreferrer">GitHub</a>
          </li>
          <li>
            <a href="https://www.researchgate.net/profile/Zhiyan-Ren-3" target="_blank" rel="noopener noreferrer">ResearchGate</a>
          </li>
          <li>
            <a href="#" target="_blank" rel="noopener noreferrer">LinkedIn (coming soon)</a>
          </li>
          <li>
            <a href="#" target="_blank" rel="noopener noreferrer">Instagram</a>
          </li>
        </ul>
      </div>

      <!-- 右列：Meta（签名 + RAG 提示） -->
      <div class="meta">
        <p class="tagline">Exploring AI × web, one small experiment at a time.</p>
        <p class="rag-hint">RAG responses are experimental and may be imperfect.</p>
      </div>
    </div>

    <!-- 底部版权 -->
    <div class="copyright">© 2025 renzhiyan</div>
  </Center>
</footer>

<style lang="scss">
  footer {
    font-size: var(--step--1);
    padding-block: var(--space-2xl);
    width: 100%;
    border-top: 1px solid var(--gray-6);
    color: var(--gray-11);
  }

  /* 两列布局（桌面）；移动端上下排列 */
  .content {
    display: grid;
    grid-template:
      "connect meta" auto /
      1fr 1fr;
    gap: var(--space-m) var(--space-xl);

    @media (width < 650px) {
      grid-template:
        "meta"
        "connect" /
        1fr;
      gap: var(--space-l);
    }
  }

  .connect { grid-area: connect; }
  .meta { grid-area: meta; }

  .title {
    font-weight: 600;
    margin-bottom: var(--space-s);
    font-size: var(--step-0);
    color: var(--gray-12);
  }

  .connect ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.35rem;
  }

  .connect a {
    color: var(--gray-11);
    text-decoration: none;
  }

  .connect a:hover {
    color: var(--gray-12);
    text-decoration: underline;
  }

  .meta {
    max-width: 38ch;
  }

  .tagline {
    margin: 0 0 .5rem 0;
    color: var(--gray-12);
  }

  .rag-hint {
    margin: 0;
    font-size: var(--step--2);
    opacity: .8;
  }

  .copyright {
    margin-top: var(--space-l);
    font-size: var(--step--2);
    opacity: .7;
  }
</style>
